import styled from 'styled-components';

import { Form as UnForm } from '@rocketseat/unform';

export default styled(UnForm)`
	margin-top: 20px;
	padding: 30px;
	border-radius: 4px;
	background: #fff;

	span {
		color: red;
		margin-top: 5px;
	}
`;
